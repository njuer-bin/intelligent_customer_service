# 测试文档：小微商户智能客服应答机器人

> 与《实施文档》配套。按里程碑分组，每个 M 列明测试项、测试要点与完成条件。
> 工具：pytest + 本地 Ollama(qwen2.5:7b)+ bge embedding；不依赖外部网络 API（可全程离线测试）。

---

## 测试层级总览
- **单元测试**：数据模型校验、分块、工具调用、路由决策解析、记忆窗口。
- **集成测试**：入库→检索→生成链路；多商户隔离；Gradio smoke。
- **端到端(E2E)**：演示商户完整问答闭环 + 未命中沉淀。
测试目录建议：`tests/unit/`, `tests/integration/`, `tests/e2e/`。

---

## M1：数据模型与脚手架
**测试项**：
1. `schemas.py` 模型实例化校验（字段缺失/类型错误抛 Pydantic 异常）。
2. 四类模型（Merchant/KnowledgeItem/Session/UnknownQuestion）字段齐全、含时间戳与 merchant_id。
3. 冒烟测试：`import sme_guard` 及各子模块 import 成功。
4. 环境可重建：`conda env create` 后用 `pytest --collect-only` 无采集错误。
**完成条件**：全部 import 通过，模型校验用例绿，pytest 集成功率 100%。
**需手动**：Conda 建环境、`ollama pull qwen2.5:7b`、`ollama pull bge-small-zh-v1.5`。

## M2：RAG 入库与基础问答
**测试项**：
1. chunker：给定中文文档输出多个带 overlap 的段；标题感知不切碎段落。
2. ingest：演示资料入库后 collection 计数正确、持久化可重载。
3. retrieve：对 3 个已知问题 query，top5 中含正确文档片段（断言命中 doc 名）。
4. generator：命中问题返回含出处标注；无数据/无依据问题返回"暂时无法回答"。
**完成条件**：检索命中断言通过；命中/未命中两类问答各有 ≥1 用例绿。
**需手动**：准备 1 套演示资料（3 个 md）。

## M3：ReAct 路由与短期记忆
**测试项**：
1. llm_client：给定 messages 返回字符串；Ollama 与 API 两分支（若有）解析格式正确。
2. decision 解析：构造含 `{decision: rag|direct|human}` 的 LLM 输出，断言解析与兜底逻辑（解析失败默认 human）。
3. 多轮连贯：第一轮问某套餐，第二轮"那价格呢"——断言能带上窗口历史，context 中包含前轮关键信息。
4. 熔断：构造连续工具/重复决策输入，断言 max_iterations 到点强制终止。
**完成条件**：三态决策用例通过；多轮连贯用例如通过；熔断边界用例绿。

## M4：工具调用与未命中沉淀
**测试项**：
1. price_lookup：正常项返回结构化价格；不存在项抛业务错误。
2. avail_lookup：返回可用时段（假数据）。
3. registry：schema 由函数签名自动生成（字段/描述/类型正确）。
4. router 触发：含工具意图输入能正确产出 tool_call 并执行、结果回填 observation。
5. 错误重试：工具抛错后模型收到错误 observation 触发修正（断言至少一次重试路径）。
6. unknowns：检索无结果或模型声明无法回答时，正确写入 UnknownQuestion（原文本+时间+merchant_id）。
**完成条件**：工具触发正确率用例、错误重试用例、未命中写库断言全部绿。

## M5：多商户隔离与 Gradio
**测试项**：
1. 商户隔离：商户 A 提问不返回商户 B 的 collection 片段（断言结果 doc 不来自 B）。
2. collection 生命周期：新增/删除商户后 collection 正确创建/移除。
3. 配置链路：入库"新增资料"后检索能命中新内容。
4. Gradio smoke：`gradio_app` 模块可实例化、`launch` 可启动（headless 模式）不抛错。
**完成条件**：隔离用例、生命周期用例、smoke 用例绿。
**需手动**：准备 2-3 套不同行业演示资料。

## M6：回归与端到端
**测试项**：
1. 全量回归：M1~M5 全部用例 + 边界/异常补充（空 query、超长输入、空知识库）。
2. E2E：选择一个演示商户，3 个高频问题依次提问，断言均返回合理结果且带出处。
3. E2E 未命中：提出一个知识库外问题，断言进入 unknowns。
4. 无幻觉检查：无依据 query 不得返回编造答案（必须"说不知道"或转人工）。
**完成条件**：`pytest` 全绿；E2E 三问 + 未命中场景通过；无幻觉断言通过。

---

## 运行方式
```
conda activate sme_guard
pytest tests/ -v          # 全量
pytest tests/unit/        # 单元
pytest tests/integration/ # 集成
pytest tests/e2e/         # 端到端(需本地模型与演示数据就绪)
```
> 注：E2E 依赖本地 Ollama 与演示数据，文档注明"需手动环境"而非代码缺陷。

## 诚实说明
测试集中演示数据均为虚构范例；测试代写目标仅为验证功能正确，不夸大系统能力、不宣称生产级效果。
