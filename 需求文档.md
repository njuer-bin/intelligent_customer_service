主要功能模块：
1. LLM客户端抽象模块：屏蔽本地Ollama与云端API差异
2. ReAct Agent路由模块：意图决策，支持RAG检索、直接回答、调用工具、引导人工
3. RAG知识库模块：文档加载、标题感知分块、向量化、检索、重排，商户向量集合隔离
4. 会话记忆模块：定长滑动窗口维护多轮对话上下文
5. 工具调用模块：套餐价格查询、预约可用性查询，工具注册、异常重试、最大迭代熔断
6. 未命中问题存储：机器人无法解答的用户问题持久保存
7. Gradio可视化界面：商户配置、对话交互、查看未命中问题
8. 测试与交付文档

## 2.3 用户特征
1. **访客用户（咨询顾客）**：到店潜在消费者，通过Web界面提问营业时间、套餐、预约等，无技术背景。
2. **小微商户运营者**：餐饮/美容/健身等个体户，无IT开发能力；上传经营资料，查看未命中问题，维护商户知识库。

## 2.4 运行环境
- Python >=3.10；Conda虚拟环境管理
- LLM：Ollama qwen2.5:7b‑instruct（本地离线），备用DeepSeek API
- Embedding：bge‑small‑zh‑v1.5
- 向量数据库：Chroma（本地持久化）
- 文档解析：pdfplumber
- Web UI：Gradio
- 数据模型：Pydantic
- 测试框架：pytest

## 2.5 设计约束
1. Agent ReAct逻辑手写编码实现，不直接使用LangChain/LlamaIndex现成Agent组件。
2. 向量库Chroma按商户ID隔离Collection，不同商户知识库严格隔离。
3. 禁止使用`eval`执行动态代码；不开放任意文件操作工具。
4. LLM输出JSON不稳定时，必须增加宽松正则、容错解析、一次重试机制。
5. 中文文档分块采用标题感知+overlap重叠策略，禁止简单按token硬切割。
6. 无检索依据时禁止编造答案，必须输出无法回答提示，防范大模型幻觉。

## 2.6 假设和依赖
1. 假设商户提供的PDF、Markdown经营资料内容真实准确。
2. 本地部署模式依赖Ollama服务正常运行，已提前拉取qwen2.5:7b‑instruct、bge‑small‑zh‑v1.5模型。
3. API模式依赖网络可访问DeepSeek接口。
4. Chroma向量数据库本地文件持久化，假设磁盘读写正常。
5. 工具模块价格、预约可用性为演示假数据，不对接真实业务后端。

---

# 3 具体需求
## 3.1 功能需求
### 3.1.1 数据模型与项目脚手架 FR‑01
FR‑01.1：代码目录严格对齐实施文档`sme_guard`目录结构，所有模块空实现可以正常import无报错。
FR‑01.2：`schemas.py`使用Pydantic定义四类数据模型：Merchant商户、KnowledgeItem知识条目、Session会话、UnknownQuestion未命中问题；包含商户ID、时间戳等必要字段。
FR‑01.3：提供`environment.yml`，支持conda一键重建项目运行环境。
FR‑01.4：冒烟测试可执行，模块导入无异常。

### 3.1.2 LLM客户端抽象 FR‑02
FR‑02.1：`llm_client.py`对外统一`chat(messages)`接口，内部兼容Ollama本地调用、DeepSeek云端API两种后端。
FR‑02.2：模型调用异常捕获，抛出可识别异常，供上层Agent处理。

### 3.1.3 RAG知识库入库与检索 FR‑03
FR‑03.1：`ingest.py`支持加载Markdown、PDF文档；pdf解析使用pdfplumber。
FR‑03.2：文档采用**标题感知分块，overlap=100字符**中文段落切分策略。
FR‑03.3：`vector_db.py`薄封装Chroma，每个商户分配独立collection；完成向量化、新增、删除。
FR‑03.4：`retrieve.py`实现查询向量化、top5片段召回、简单相似度重排序。
FR‑03.5：生成回答附带来源出处标记`[doc: 段落]`；无检索结果输出“暂时无法回答”，禁止编造内容。

### 3.1.4 ReAct Agent路由模块 FR‑04
FR‑04.1：`agents/router.py`手写ReAct调度，输出结构化决策：`{decision: rag|direct|human, reason}`。
FR‑04.2：决策分支：RAG检索回答 / 直接回答 / 引导人工客服。
FR‑04.3：支持工具调用分支，调用注册工具，获取观察结果继续推理。
FR‑04.4：设置`max_iterations`最大迭代熔断，防止Agent死循环。

### 3.1.5 短期会话记忆 FR‑05
FR‑05.1：`memory/session.py`实现定长滑动窗口，保存最近若干轮对话上下文。
FR‑05.2：会话绑定商户ID；会话可以重置清空上下文。
FR‑05.3：多轮追问可以理解上文指代，实现连贯对话。

### 3.1.6 工具调用模块 FR‑06
FR‑06.1：实现工具注册器`registry.py`，工具注册格式包含可调用函数、json schema。
FR‑06.2：实现`price_lookup.py`套餐价格查询、`avail_lookup.py`预约可用性查询，内部使用演示假数据。
FR‑06.3：工具执行异常，允许最多一次重试；异常结果回传给Agent作为observation。
FR‑06.4：工具调用同样受全局`max_iterations`熔断保护。

### 3.1.7 未命中问题沉淀 FR‑07
FR‑07.1：`store/unknowns.py`，当Agent判定无法回答用户问题，将原始问题、商户ID、时间戳持久保存。
FR‑07.2：支持读取全部未命中问题列表供商户查看。

### 3.1.8 Gradio可视化界面 FR‑08
FR‑08.1：`ui/gradio_app.py`使用Gradio Blocks构建页面，包含商户配置面板、对话页面、未命中问题视图。
FR‑08.2：支持选择商户，上传文档入库；商户之间知识库互相隔离。
FR‑08.3：完整流程：配置商户→上传资料→访客对话→查看未命中问题。

### 3.1.9 测试与交付文档 FR‑09
FR‑09.1：pytest测试套件，覆盖数据层、RAG检索、Agent路由、工具调用、记忆、商户隔离。
FR‑09.2：README.md，包含简介、Mermaid架构图、5分钟快速开始、技术取舍、系统局限性。
FR‑09.3：准备端到端演示脚本与演示商户资料。

## 3.2 外部接口需求
### 3.2.1 用户界面接口
Gradio Web页面，包含商户配置面板、对话聊天窗口、未命中问题列表视图。

### 3.2.2 软件接口
1. Ollama接口：本地HTTP接口，完成大模型推理与Embedding计算。
2. DeepSeek API：HTTP云端大模型接口（备用）。
3. Chroma向量库本地文件接口：向量新增、查询、删除。

## 3.3 非功能需求
### 3.3.1 性能需求
NFR‑01.1：单轮问答在本地7B模型环境，完整生成响应时间可接受，无硬性高并发指标，面向单机演示场景。
NFR‑01.2：文档入库分块向量化随文档大小线性增长。

### 3.3.2 可靠性需求
NFR‑02.1：LLM输出JSON解析失败具备容错策略：宽松正则解析+最多一次重试。
NFR‑02.2：Agent推理、工具调用具备max_iterations熔断，杜绝死循环。
NFR‑02.3：RAG无匹配知识片段，禁止编造业务答案，返回无法回答提示。

### 3.3.3 可用性需求
NFR‑03.1：Gradio界面操作简单，商户无需编程即可上传资料查看未命中问题。
NFR‑03.2：项目提供环境配置文档，支持conda一键搭建环境。

### 3.3.4 可维护性需求
NFR‑04.1：模块职责清晰，llm_client、rag、agent、tools、memory解耦，方便替换底层模型。
NFR‑04.2：关键业务路径具备pytest单元、集成测试。

### 3.3.5 可扩展性需求
NFR‑05.1：工具注册器设计，后续可新增更多业务工具。
NFR‑05.2：数据模型基于Pydantic，方便扩展字段。

## 3.4 数据需求
- 持久化数据：Chroma向量集合、Merchant商户信息、KnowledgeItem知识条目、UnknownQuestion未命中问题。
- 临时数据：Session会话对象，会话结束后不需要持久化全部对话历史（演示版）。

---

# 4 其他需求
## 4.1 安全性需求
1. 项目禁止`eval`动态代码执行，规避代码注入风险。
2. 不同商户向量集合隔离，防止A商户查询返回B商户知识库内容。
3. 本项目为教育演示项目，不面向公网暴露，不做严格账号权限；生产使用需要额外增加鉴权。

## 4.2 业务规则
BR‑01：没有知识库依据，Agent不能编造商户业务信息，回复“暂时无法回答”。
BR‑02：Agent ReAct、工具调用必须受max_iterations熔断保护，防止无限循环。
BR‑03：解析LLM输出JSON失败，执行容错重试逻辑，不能直接崩溃。
BR‑04：检索无结果的用户问题，必须写入UnknownQuestion记录，供商户迭代知识库。
BR‑05：工具价格、预约可用性均为演示假数据，Agent输出时不伪装为真实线上系统。

---

# 5 迭代与里程碑规划（对齐实施文档Sprint）
| 里程碑 | Sprint | 时间区间 | 核心产出 |
|---|---|---|---|
| M1 需求+脚手架+数据模型 | S1 | 09‑14 ~ 09‑27 | 需求清单，项目骨架，Pydantic模型，conda环境配置 |
| M2 RAG入库+基础问答(MVP) | S2 | 09‑28 ~ 10‑11 | ingest、retrieve、vector_db，CLI最小问答闭环 |
| M3 ReAct 路由+短期记忆 | S3 | 10‑12 ~ 10‑25 | llm_client、agent/router、memory会话多轮记忆 |
| M4 工具调用+未命中沉淀 | S4 | 10‑26 ~ 11‑01 | price/avail工具、工具注册器、unknowns落库 |
| M5 多商户管理+Gradio整合 | S5 | 11‑02 ~ 11‑08 | 商户隔离校验，完整Gradio UI |
| M6 测试收尾+README+演示 | S6 | 11‑09 ~ 11‑15 | pytest全套测试、README文档、端到端演示 |

> 每Sprint周末做review；提前完成进入下一里程碑；延期不降低交付质量。

---

# 6 验收总准则
1. 代码目录与实施文档给出`sme_guard`目录完全一致，模块import无报错。
2. M1‑M6每个里程碑的验收点全部通过。
3. RAG回答附带来源标记，无依据问题不编造，正确沉淀未命中问题。
4. Agent ReAct决策分支（rag/direct/human/tool）可正常触发，迭代熔断生效。
5. 商户之间知识库隔离，A商户查询不会拿到B商户资料片段。
6. Gradio完整流程跑通，可上传文档、对话、查看未命中。
7. pytest测试套件执行，核心用例全部通过；README文档完整，可完成端到端演示。

---

# 附录A：用例规约
## 用例1：访客用户发起咨询
**参与者**：访客用户
**前置条件**：Gradio服务启动，已选择目标商户
**后置条件**：返回回答 / 无法回答提示 / 引导人工；无法回答的问题写入未命中库
**基本流**
1. 用户输入业务问题发送
2. 会话模块加载当前上下文
3. ReAct Agent Router做意图决策
4. 执行分支：RAG检索 / 直接回答 / 调用业务工具 / 引导人工
5. 生成回答返回前端
6. 如果判定无法回答，将原始问题写入UnknownQuestion存储
**扩展流**
*a. LLM返回JSON格式异常 → 执行容错解析+一次重试。

## 用例2：商户上传经营知识库文档
**参与者**：小微商户运营者
**前置条件**：Gradio配置面板打开，选定当前商户
**后置条件**：文档解析分块向量化，存入该商户独立Chroma collection
**基本流**
1. 商户上传PDF或者Markdown文件
2. 系统加载文档、标题感知分块、生成Embedding向量
3. 写入该商户专属collection
4. 界面提示上传完成

## 用例3：Agent调用业务工具查询套餐价格
**参与者**：Agent系统
**前置条件**：用户提问涉及套餐价格
**后置条件**：获取工具返回的假数据结果，融入回答
**基本流**
1. Router识别需要调用price_lookup工具
2. 通过registry执行工具函数
3. 获取工具返回observation，交给大模型生成最终回答
**扩展流**
*a. 工具执行抛出异常 → 重试一次；重试失败将异常信息返回Agent。

## 用例4：商户查看未命中问题
**参与者**：小微商户运营者
**前置条件**：存在多条UnknownQuestion记录
**后置条件**：界面展示全部未命中用户问题、商户ID、时间戳
**基本流**
1. 在Gradio切换至未命中视图
2. 系统读取store/unknowns存储的数据
3. 在页面列表展示问题记录
