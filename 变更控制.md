# 变更控制日志

## v2.1.0 - 2026-09-20
- **功能**：M5 多商户管理 + Gradio 整合 - 完整可视化交互界面
  - **vector_db.py 增强**：
    - 新增 `list_merchants()`：列出所有商户及文档统计
    - 新增 `get_merchant_info(merchant_id)`：获取单商户详情
    - 新增 `merchant_exists(merchant_id)`：检查商户是否存在
  - **store/unknowns.py (新建)**：未命中问题持久化存储
    - 按商户隔离 JSON 文件存储 (`unknowns/merchant_id.json`)
    - 线程安全的增删改查：`add()`、`list()`、`count()`、`clear()`
    - 全局实例 `unknown_store` 及便捷函数
  - **ui/gradio_app.py (完整实现)**：三页签交互界面
    - **配置面板**：商户创建/删除/切换、资料上传入库 (Markdown/PDF/TXT)
    - **对话页**：商户选择、多轮对话、出处标注、重排序开关
    - **记录页**：未命中问题列表（按商户筛选/清空），支持商户迭代完善资料
  - **文件修改**：`sme_guard/rag/vector_db.py`、`sme_guard/store/unknowns.py` (新建)、`sme_guard/ui/gradio_app.py` (重写)

## v2.0.0 - 2026-09-20
- **重构**：RAG 核心检索链路全面优化，从基础版演进为多阶段工程化架构
  - **问题**：初版 RAG 仅包含 Embedding + 向量检索 + LLM，存在知识召回不准、Chunk 信息割裂、上下文噪声大、响应延迟高等问题
  - **改进**：
    - **知识库结构化**（V2）：按业务主题（套餐、服务、门店、预约、售后）重新组织 Markdown 文档，补全缺失字段（课时、有效期、适用人群、注意事项等），解决“知识库无答案导致 RAG 无法作答”问题
    - **语义化 Chunking**（V3）：按 Markdown 层级与业务语义切分，保持单个业务实体（如某套餐完整信息）在一个 Chunk 内，避免固定长度切分导致价格/有效期/课时被拆散，提升检索完整性
    - **Embedding 优化**（V4）：选用中文语义理解更强的模型，提升 Query 与 Document 语义匹配度，解决“怎么预约”vs“微信/电话/到店”等同义表达召回不足问题
    - **Top-K 召回策略调整**（V5）：采用“宽召回 + 精排”策略，扩大候选集覆盖率，避免 K 过小漏掉答案、K 过大引入噪声
    - **引入 Reranker 二阶段检索**（V6）：在 `sme_guard/rag/retrieve.py` 集成 CrossEncoder 重排序，Vector Search 负责高召回，Reranker 负责高相关，形成“Recall First, Precision Later”架构
    - **Context Selection 优化**（V7）：Rerank 后进一步筛选 Top-N，去重、去低相关、控制长度，减少无关 Chunk 进入 Prompt
    - **Prompt 约束优化**（V8）：强化“仅依据检索结果回答、不编造、按问题范围作答”约束，提升生成稳定性
    - **本地 LLM 模型测试与选型**（V9）：基于 Ollama 对比多模型（qwen2.5:7b 等），按推理速度/回答质量/资源占用平衡选型，明确 Embedding 与 LLM 职责分离
    - **分阶段性能分析**（V10）：拆解 T_total = T_embedding + T_retrieval + T_rerank + T_context + T_LLM，定位瓶颈并针对性优化模型大小、候选数、Context 长度
  - **效果**：建立从知识库、Chunking、Embedding、召回、重排、Context、Prompt、LLM、性能的完整优化链路；回答准确率显著提升，系统响应速度得到优化
  - **文件修改**：`sme_guard/rag/ingest.py`、`sme_guard/rag/retrieve.py`、`sme_guard/rag/mqe.py`、`sme_guard/llm_client.py`、`sme_guard/monitoring.py`、`sme_guard/api.py`、知识库 Markdown 文档

## v1.0.5 - 2026-09-17
- **功能**：M4-3 检索重排序 - 引入重排序模型提升精度
  - **内容**：在 `sme_guard/rag/retrieve.py` 中集成 CrossEncoder 重排序模型
  - **实现**：使用 `sentence-transformers` 的 `cross-encoder/ms-marco-MiniLM-L-6-v2` 模型
  - **参数**：`use_reranker` 标志位控制是否启用重排序
  - **效果**：对检索结果重新评分排序，显著提升相关性召回精度
  - **新增**：`rerank()` 函数供外部调用
  - **文件修改**：`sme_guard/rag/retrieve.py` 完整重构，新增重排序逻辑

- **功能**：M4-4 系统监控 - 添加性能监控和报警
  - **内容**：在 `sme_guard/monitoring.py` 创建系统监控模块
  - **指标**：请求延迟、成功率、错误率、缓存命中率、组件统计
  - **阈值配置**：`max_avg_latency`、`max_error_rate`、`min_cache_hit_rate`
  - **告警回调**：支持注册自定义告警回调函数
  - **汇总报告**：`get_summary()` 返回完整性能概览
  - **监控端点**：`GET /monitor` 提供 REST API 访问监控数据
  - **记录集成**：`multi_query_retrieve()` 和 `retrieve()` 自动记录指标
  - **文件修改**：`sme_guard/monitoring.py` (新建)、`sme_guard/rag/retrieve.py`、`sme_guard/rag/mqe.py`

- **功能**：M4-5 API 接口 - 提供RESTful API接口
  - **内容**：在 `sme_guard/api.py` 创建 RESTful API 服务
  - **端点**：
    - `GET /health` - 健康检查
    - `POST /query` - 处理查询 (含 MQE + 可选重排序)
    - `GET /sessions` - 列出会话
    - `POST /sessions` - 创建会话
    - `GET /sessions/{id}` - 获取会话详情
    - `GET /monitor` - 获取监控数据
  - **框架**：基于 Flask，`flask>=3.0` 依赖
  - **线程安全**：使用锁保护共享资源访问
  - **文件修改**：`sme_guard/api.py` (新建)、`pyproject.toml` 新增 flask 依赖

- **更新**：新增自定义测试用例至 `scripts/test_my_questions.py`
  - **新增**：增加 5 条边界与扩展测试查询，覆盖节假日、团课、退改规则、团购价格及未知内容查询
  - **目的**：增强测试覆盖率，验证系统在各类查询下的表现
  - **文件修改**：`scripts/test_my_questions.py` 第 58-71 行

## v1.0.4 - 2026-09-17
- **优化**：将检索阈值 `MIN_SCORE` 从 `0.3` 降低至 `0.15`
  - **原因**：MQE 扩展后最高分（如“几点开门”~0.23）仍低于 0.3 阈值，导致合理查询被拒答
  - **影响**：召回更多查询，提升用户体验（部分鉴于文档语义匹配局限仍可能拏答）
  - **文件修改**：`scripts/quick_test2.py` 第 18 行

- **优化**：接入 DeepSeek API 并集成至统一 LLM 客户端
  - **实现**：在 `sme_guard/llm_client.py` 中添加双模式支持（Ollama 本地模型 / DeepSeek API）
  - **环境变量**：设置 `DEEPSEEK_API_KEY= 即可使用 Deepseek 模型 `deepseek-chat`
  - **回退机制**：未检测到 Key 时自动回退至 Ollama `qwen2.5:7b`
  - **文件修改**：`sme_guard/llm_client.py` 全量重写

- **优化**：将向量模型由 `qllama/bge-small-zh-v1.5:latest` 更换为 `qwen3-embedding:4b`
  - **变更**：`sme_guard/rag/ingest.py` 第 21 行、`sme_guard/rag/retrieve.py` 第 34-36 行、测试断言更新
  - **维度变化**：输出由 512 维改为 2560 维，检索精度提升
  - **文件修改**：`sme_guard/rag/ingest.py`、`sme_guard/rag/retrieve.py`、`tests/unit/test_retrieve.py`

- **更新**：新增自定义测试用例至 `scripts/test_my_questions.py`
  - **新增**：增加 5 条边界与扩展测试查询，覆盖节假日、团课、退改规则、团购价格及未知内容查询
  - **目的**：增强测试覆盖率，验证系统在各类查询下的表现
  - **文件修改**：`scripts/test_my_questions.py` 第 58-71 行

## v1.0.0 - 2026-09-16
- 初始提交：M1（模型+Schema）+ M2（RAG pipeline）完成，44/44 tests passing
- 功能：MQE 多查询扩展模块上线
- 编码修复：`scripts/quick_test2.py` / `quick_test3.py` 加入 `io.TextIOWrapper` 解决终端 GBK/UTF-8 显示问题